apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-logger
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git@github.com:Nik577/myapp-gitops.git'
    targetRevision: main
    path: apps/auth-logger
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: myapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
